option_type = int(input('What type of Option Order? Long Call, Short Call, Long Put, or Short Put. O for long call, 1 for short call, ...etc'))


current_price = input('What is the current price of the underlying asset?')
current_price = float(current_price)

strike_price = input('What is the strike price?')
strike_price = float(strike_price)

premium = input('What is the premium per share?')
premium = float(premium)



